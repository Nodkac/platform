apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "demo.fullname" . }}-dashboard
  labels:
    grafana_dashboard: "1"    # Grafana's sidecar auto-imports ConfigMaps with this label
data:
  demo.json: |-
{{ .Files.Get "dashboards/demo.json" | indent 4 }}